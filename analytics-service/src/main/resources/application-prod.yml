spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    baseline-on-migrate: false

logging:
  level:
    root: WARN
    com.caloryhive.business: INFO
