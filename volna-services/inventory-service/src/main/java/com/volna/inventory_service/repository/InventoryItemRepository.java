package com.volna.inventory_service.repository;
import com.volna.inventory_service.entity.InventoryItem;import com.volna.inventory_service.enums.InventoryStatus;import org.springframework.data.domain.*;import org.springframework.data.jpa.repository.JpaRepository;import java.time.LocalDate;import java.util.*;
public interface InventoryItemRepository extends JpaRepository<InventoryItem,Long>{Page<InventoryItem> findByRestaurantId(UUID r,Pageable p);Page<InventoryItem> findByRestaurantIdAndNameContainingIgnoreCase(UUID r,String n,Pageable p);List<InventoryItem> findByRestaurantIdAndStatus(UUID r,InventoryStatus s);List<InventoryItem> findByRestaurantIdAndExpiryDateBetween(UUID r,LocalDate a,LocalDate b);Optional<InventoryItem> findByIdAndRestaurantId(Long id,UUID r);boolean existsBySkuAndRestaurantId(String sku,UUID r);}
